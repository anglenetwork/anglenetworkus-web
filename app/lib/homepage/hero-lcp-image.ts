/** Shared with hero `ImageRenderer` and LCP preload — keep in sync. */
export const HOMEPAGE_HERO_LCP_IMAGE = {
  width: 1000,
  height: 563,
  quality: 60,
  sizes: "(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 720px",
} as const;
