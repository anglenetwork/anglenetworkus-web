export const TAGS_GLIMPSE_TAG_COUNT = 4;

/** Fetch extra tags so we can still fill four slots when some tags lack articles or share the same latest story. */
export const TAGS_GLIMPSE_TAG_POOL = 12;
