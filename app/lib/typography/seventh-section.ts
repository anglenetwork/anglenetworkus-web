/** Homepage seventh section — hover: headline fades to ink-soft. */
const angleTitleHover = "transition-colors group-hover:text-angle-inkSoft";

/** "Featured Stories" section heading with horizontal rule. */
export const featuredStoriesHeading =
  "font-sans text-[22px] font-bold tracking-[-0.3px] text-angle-ink";

/** Carousel card category eyebrow (e.g. POLITICS). */
export const featuredStoriesEyebrow =
  "mb-3 mt-4 font-sans text-[11px] font-bold uppercase tracking-[0.12em] text-angle-red";

/** Carousel card headline beneath the image. */
export const featuredStoriesHeadline = `font-display text-[17px] font-semibold leading-[1.28] tracking-[-0.2px] text-angle-ink ${angleTitleHover}`;
