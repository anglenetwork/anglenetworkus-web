import { articleTitleLink } from "./article-links";

/** ThirdSection — featured column lead story title (left and right) */
export const featuredColumnTitle = `pt-2 font-sans text-lg font-semibold leading-tight tracking-normal md:text-2xl xl:font-semibold xl:text-gray-900 ${articleTitleLink}`;

/** ThirdSection — small story row title beneath featured column */
export const secondaryRowTitle = `font-sans text-base font-normal leading-snug tracking-normal md:text-lg xl:font-normal ${articleTitleLink}`;

/** ThirdSection — mainThirdSection side card title (image + headline) */
export const thirdSectionSideCardTitle = `font-sans font-semibold text-neutral-900 text-xl leading-snug tracking-tight ${articleTitleLink}`;

/** ThirdSection — mainThirdSection lead column hero title */
export const thirdSectionMainHeroTitle = `font-sans font-semibold text-2xl text-neutral-900 leading-snug tracking-tight ${articleTitleLink}`;
