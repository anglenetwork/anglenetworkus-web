import { articleTitleLink } from "./article-links";

/** ArticleFamilyCard — compact / large layout title */
export const articleFamilyCardTitle = `mb-2 font-display text-base font-semibold leading-snug tracking-normal text-news-text md:text-lg ${articleTitleLink}`;

/** ArticleFamilyCard — rail layout title */
export const articleFamilyCardTitleRail = `line-clamp-2 font-display text-sm font-semibold leading-snug tracking-normal text-news-text ${articleTitleLink}`;

/** ArticleFamilyCard — hero tile overlay title (fifth homepage section carousel) */
export const articleFamilyHeroTileTitle = `mb-2 font-display font-semibold text-white text-xl leading-snug tracking-tight ${articleTitleLink}`;

/** Homepage — EditorialRailsSection opinion overlay card headline */
const editorialOpinionRailTitle = `break-words font-bold font-display text-news-text text-xl leading-[1.08] tracking-tight sm:text-2xl md:text-2xl lg:text-4xl ${articleTitleLink}`;
